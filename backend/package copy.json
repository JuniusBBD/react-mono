{
  "name": "aws-feature-flag",
  "private": true,
  "version": "0.0.1",
  "description": "Feature flag system using AWS services",
  "license": "MPL-2.0",
  "scripts": {
    "test": "vitest",
    "start": "ts-node --transpile-only index.ts",
    "dev": "ts-node-dev --respawn --transpile-only --exit-child --watch aws index.ts"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.5.7",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2",
    "vitest": "^1.5.0"
  },
  "dependencies": {
    "aws-sdk": "^2.1691.0",
    "express": "^4.21.0"
  },
  "optionalDependencies": {
    "@rollup/rollup-linux-x64-gnu": "^4.13.0"
  }
}
